/**
 * OPERADORES DE COMPARAÇÃO
 * > maior que
 * >= maior que ou igual a
 * < menor que
 * <= menor que ou igual a
 * == igualdade (valor) NÂO USAR ******************
 * === igualdade estrita (valor e tipo)
 * != diferente (valor) NÂO USAR ******************
 * !== diferente estrito (valor e tipo)
 */

const num1 = 10;
const num2 = 11;
const comp = 10 >= 5;
console.log(comp);
console.log(10 > 5);  